{
  "name": "german-flow-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "frontend:start": "npm run start --prefix frontend",
    "frontend:build": "npm run build --prefix frontend",
    "frontend:test": "npm run test --prefix frontend",
    "frontend:build:pages": "npm run build:pages --prefix frontend",
    "frontend:deploy:pages": "npm run deploy:pages --prefix frontend"
  }
}

